# Red

Write ONE failing test for: {user_request}

## Rules
- One test only — do not write any production code
- The test must fail for a semantic reason (not a syntax or import error)
- All existing tests must still pass
- Run the test suite and show the failure output before stopping
- Test file before production file
