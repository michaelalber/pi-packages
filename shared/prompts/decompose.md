# Decompose

Break the task below into atomic steps before executing.

## Rules
1. List numbered steps — one sentence each, one file per step
2. Mark steps with external dependencies (DB, API, config, migration)
3. Identify which step to validate with a test
4. Stop after the list — wait for confirmation before executing

## Task
{user_request}
