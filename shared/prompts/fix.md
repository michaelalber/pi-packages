# Fix

Fix the bug described below.

## Rules
1. Read the failing code — understand the root cause before writing anything
2. Call `search_knowledge` if the fix involves an unfamiliar library or pattern
3. Write the minimal change that fixes the bug — no refactoring, no unrelated cleanup
4. If the fix requires changing a test, explain why before changing it
5. Mark all generated code: `// <AI-Generated START>` ... `// <AI-Generated END>`

## Task
{user_request}

## Relevant code
{code_context}
