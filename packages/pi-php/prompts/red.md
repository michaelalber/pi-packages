# Red (PHP / Laravel)

Write ONE failing Pest test for: {user_request}

## Rules
- One test only — do not write any production code
- The test must fail for a semantic reason (not a parse error)
- All existing tests must still pass
- Run `php artisan test` and show the failure output before stopping
- Place test in `tests/Unit/` or `tests/Feature/`; use `it('...')` or `test('...')` syntax
