# Green (PHP / Laravel)

Make this failing Pest test pass.

## Task
{user_request}

## Rules
- Write the MINIMUM PHP code that makes the test pass — nothing it does not require
- No refactoring, no error handling, no untested branches
- `declare(strict_types=1)` at the top of every new file; type-hint all parameters and returns
- Run `php artisan test` and show all tests passing before stopping
