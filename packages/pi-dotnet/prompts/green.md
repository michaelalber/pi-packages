# Green (.NET)

Make this failing xUnit test pass.

## Task
{user_request}

## Rules
- Write the MINIMUM C# code that makes the test pass — nothing it does not require
- No refactoring, no error handling, no untested branches
- `async`/`await` end-to-end — never `.Result` or `.Wait()`
- Run `dotnet test` and show all tests passing before stopping
