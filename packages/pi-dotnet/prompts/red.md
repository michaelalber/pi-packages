# Red (.NET)

Write ONE failing xUnit test for: {user_request}

## Rules
- One test only — do not write any production code
- The test must fail for a semantic reason (not a build error)
- All existing tests must still pass
- Run `dotnet test` and show the failure output before stopping
- Test class name: `<Subject>Tests`; method name: `<Verb>_<Condition>_<ExpectedOutcome>`
