# Green (Rust)

Make this failing Rust test pass.

## Task
{user_request}

## Rules
- Write the MINIMUM Rust code that makes the test pass — nothing it does not require
- No refactoring, no untested branches, no speculative error handling
- No `unwrap()` or `expect()` — use `?` with `thiserror` or `anyhow`
- Run `cargo test` and show all tests passing before stopping
