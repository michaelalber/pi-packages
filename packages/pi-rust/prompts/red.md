# Red (Rust)

Write ONE failing test for: {user_request}

## Rules
- One test only — do not write any production code
- The test must fail for a semantic reason (not a compile error)
- All existing tests must still pass
- Run `cargo test` and show the failure output before stopping
- Unit tests: `#[cfg(test)]` module in the same file; integration tests: `tests/` directory
